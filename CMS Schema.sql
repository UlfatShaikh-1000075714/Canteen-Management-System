-- CREATE DATABASE ULFATCMS;
-- USE ULFATCMS;

-- CREATE TABLE CUSTOMER(
-- CUSTOMER_ID INT NOT PRIMARY KEY,
-- CUSTOMER_NAME VARCHAR(50) NOT NULL,
-- PHONE_NO VARCHAR(10) NOT NULL,
-- WALLET_BALANCE INT INT,
-- ADDRESS VARCHAR(30),
-- USERNAME VARCHAR(25),
-- PASSWORD VARCHAR(25),
-- CONSTRAINT C_UK  unique key(USERNAME),
-- CONSTRAINT C_UK  unique key(PASSWORD)
-- );

-- CREATE TABLE VENDOR(
-- VENDOR_ID INT PRIMARY KEY,
-- VENDOR_NAME VARCHAR(25)NOT NULL,
-- PHONE_NO VARCHAR(10) NOT NULL,
-- ADDRESS VARCHAR(30) NOT NULL,
-- USERNAME VARCHAR(25),
-- PASSWORD VARCHAR(20),
-- CONSTRAINT C_UK  unique key(USERNAME),
-- CONSTRAINT C_UK  unique key(PASSWORD)
-- );

-- CREATE TABLE FOOD(
-- FOOD_ID INT PRIMARY KEY,
-- FOOD_TYPE VARCHAR(2) NOT NULL,
-- FOOD_NAME VARCHAR(20) NOT NULL,
-- CALORIE INT NOT NULL, 
-- AMOUNT INT NOT NULL,
-- VENDOR_ID INT ,
-- CONSTRAINT FK FOREIGN KEY (VENDOR_ID) REFERENCES VENDOR(VENDOR_ID)
-- );

-- CREATE TABLE ORDER_DETAILS(
-- ORDER_NO INT PRIMARY KEY,
-- VEN_ID INT NOT NULL,
-- CUS_ID INT ,
-- F_ID INT ,
-- QUANTITY INT NOT NULL,
-- ETA DATETIME ,
-- DATE_AND_TIME DATETIME NOT NULL,
-- ORDER_VALUE INT NOT NULL,
-- ORDER_STATUS VARCHAR(100) NOT NULL,
-- CONSTRAINT FK1 FOREIGN KEY (VEN_ID) REFERENCES FOOD(VENDOR_ID),
-- CONSTRAINT FK2 FOREIGN KEY (CUS_ID) REFERENCES CUSTOMER(CUSTOMER_ID),
-- CONSTRAINT FK3 FOREIGN KEY (F_ID) REFERENCES FOOD(FOOD_ID)

-- );